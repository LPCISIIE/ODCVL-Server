{% extends 'layout.twig' %}

{% block body %}

<div class="container">
    <ol class="breadcrumb">
        <li><a href="{{ path_for('home') }}">Home</a></li>
        <li class="active">Items</li>
    </ol>
    <div class="page-header">
        <h2>Items <a href="{{ path_for('item.add') }}" class="btn btn-default pull-right">Add</a></h2>
    </div>
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>#</th>
            <th>Code</th>
            <th>Product</th>
            <th>Purchase date</th>
            <th class="text-right">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for item in items %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ item.code }}</td>
                <td>{{ item.product.name }}</td>
                <td>{{ item.purchased_at|date('d/m/Y') }}</td>
                <td class="text-right">
                    <a href="{{ path_for('item.edit', {'id': item.id}) }}" class="btn btn-default btn-sm" title="Edit">
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                    <a href="{{ path_for('item.delete', {'id': item.id}) }}" class="btn btn-danger btn-sm" title="Delete">
                        <span class="glyphicon glyphicon-remove"></span>
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}
